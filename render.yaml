services:
  - type: web
    name: toolkit-pro
    env: docker
    dockerfilePath: ./Dockerfile
    plan: free
    autoDeploy: true
    healthCheckPath: /api/health
  - type: worker
    name: toolkit-pro-worker
    env: docker
    dockerfilePath: ./Dockerfile.worker
    plan: free
    autoDeploy: true
